# ===================== REDIS (LOCALHOST SIDE-CAR) =====================
spring.data.redis.host=localhost
spring.data.redis.port=6379
spring.data.redis.password=
spring.cache.type=redis

# ===================== JPA / HIBERNATE =====================
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false

# Tăng tốc độ startup kết nối DB từ xa
spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults=false
spring.data.jpa.repositories.bootstrap-mode=deferred

# ===================== LOGGING =====================
# Kafka logs are no longer needed
logging.level.com.example.SmartCV=INFO
logging.level.org.springframework.data.redis=INFO

# App specific settings
app.ai.free-plan-daily-limit=50

# Tắt lazy-initialization để tránh nghẽn cổ chai khi request đầu tiên tới (gây 502 Bad Gateway)
# Spring Boot sẽ khởi tạo mọi bean lúc startup, giúp app sẵn sàng xử lý request ngay lập tức
spring.main.lazy-initialization=false
